/** @type {import('tailwindcss').Config} */
module.exports = {
    // ESSENCIAL: Diz ao Tailwind onde escanear as classes (incluindo as classes de colchetes)
    content: [
        "./*.html",
    ],
    theme: {
        extend: {},
    },
    plugins: [],
}